name: ancpbids_mimosa
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # ancpBIDS (depuis ton zip décompressé)
      # -> tu installeras avec pip -e (voir commandes)
      - packaging
      - pandas
      - matplotlib
      - tifffile
      - nibabel
      - numpy
      # CZI reader
      - pylibCZIrw
